# csvparser

flags:
  -limit int
        1000 (default 1000)
  -maxObject int
        20 (default 20)
  -result string
        result.csv (default "./result.csv")
  -source string
        source (default "./source")
